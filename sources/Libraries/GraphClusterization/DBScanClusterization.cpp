#include "DBScanClusterization.h"

#include "BFS.h"

namespace GraphClusterization
{
    std::unique_ptr<Clusterization> CreateGreedyClusterization(const Graphs::Graph& i_graph, size_t i_epsilon, size_t i_min_neighborhood_size)
    {
        return nullptr;
    }
}